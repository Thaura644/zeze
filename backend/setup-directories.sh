# Create required directories for backend processing
mkdir -p temp uploads

# Create .gitkeep files to ensure directories are tracked
touch temp/.gitkeep
touch uploads/.gitkeep

echo "Backend directories created successfully"